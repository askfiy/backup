// ABCDEF

let userName: string = "Jack";

function func(x: number, y: number): number {
  return x + y;
}

console.log(userName);
console.log(func(1, 2));

// I think they are just wrongly marked because when I press `[s` and `]s` they don't jump to the wrong marked word.
